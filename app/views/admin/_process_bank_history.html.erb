<%= semantic_form_for :process_bank_history, :url => { :action => "process_bank_history" } do |f| %> 
    <%= f.input :file, :as => :file, :label => false %>
    <div class="buttons">
    <%= f.actions do %>
        <%= f.action :submit, :as => :button %>
    <% end %>
    </div>
<% end %>  