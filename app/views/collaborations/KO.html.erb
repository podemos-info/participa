<% content_for :title, t("podemos.collaboration.title") %>

<div class="content-content cols">
  <div class="row">
    <div class="col-b-4a12">
      <%= content_tag(:h2, t("podemos.collaboration.title")) %>

      <%= render partial: "steps", locals: {step: 3} %>

      <%= error_box t("podemos.collaboration.ko.title"), false do %>
          <p><%= @order.error_message %></p>
      <% end %>
      <p><%= t("podemos.collaboration.ko.contact") %></p>
      <div class="buttonbox">
        <%= link_to "Intentar pagar nuevamente", confirm_collaboration_path, class: 'button' %>
      </div>
    </div>
  </div>
</div>
