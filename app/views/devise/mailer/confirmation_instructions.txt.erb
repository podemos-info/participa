<%= t('podemos.devise.mailer.welcome') %> <%= @email %>

<%= t('podemos.devise.mailer.confirm.p1') %>

<%= link_to t('podemos.devise.mailer.confirm.link'), confirmation_url(@resource, confirmation_token: @token) %>

<%= confirmation_url(@resource, confirmation_token: @token) %>
