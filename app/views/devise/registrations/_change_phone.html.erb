<h2>
  <%= t("podemos.user.change_phone") %>
</h2>

<section class="content-text">

  <% if resource.can_change_phone? %>
    <p>Validáchesche co teléfono móbil <b><%= resource.phone_national %></b> de <b><%= resource.phone_country_name %></b> (prefixo internacional <%= resource.phone_prefix %>). </p>
    <div class="buttonbox">
      <%=
          button_to(
                  t("podemos.user.change_phone"),
                  sms_validator_step1_path,
                  method: :get,
                  class: "button"
          )
      %>
    </div>
  <% else %>
    <p>Confirmáchelo no últimos tres meses, así que aínda non podes cambialo.</p>
  <% end %>
</section>
