<%- province_voting ||= false %>
<div id="js-registration-user_<%=field%>-wrapper" class="input required form-group">
  <%= label :user, :province, class: "control-label", required: :required do %>
    <%= title %>
    <% if required %><abbr title="requerido">*</abbr><% end %>
  <% end %>
  <span class="form-wrapper">
  <% if province_voting %>
      <% provinces = get_voting_provinces %>
  <% else %>
      <% provinces = get_provinces(country) %>
  <% end %>
  <% if provinces.length>0 
      first_options = if required
                        "<option></option>"
                      else  
                        "<option></option><option value='-' "+(province=='-'? ' selected="selected"':'')+">-- Ninguna --</option>"
                      end
      %>
      <%= select_tag "user[#{field}]", first_options.html_safe+options_from_collection_for_select(provinces, "code", "name", selected: province, disabled: (disabled ? [] : options_filter)), {disabled: disabled, required: required, data: {placeholder: "-- Elixe unha provincia --", allowClear: true} } %>
  <% else %>
    <%= text_field(:user, field, class: "form-control input-xl") %>
  <% end %>
  </span>
</div>
