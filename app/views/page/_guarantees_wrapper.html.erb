<% content_for :title, title %>
<div class="content-content cols">
  <div class="row">
    <div class="col-b-4a12">
      <%= content_tag(:h2, title) %>
      <%= link_to "Volver a Comisión de Garantías Democráticas", guarantees_path %>
      <%= render partial: "gravity_iframe", locals: { domain: "podemos.info", basepath: "/wordpress", form_id: form_id, height: height } %>
    </div>
  </div>
</div>
