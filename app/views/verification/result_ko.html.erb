<% content_for :title, t('verification.alerts.ko.title') %>

<div class="content-content cols">
  <div class="row">
    <h2><%= t('verification.admin') %></h2>
    <div class="col-b-4a12">
      <%= error_box "" do %>
        <div class="box-large">
          <span class="title"> <%= t('verification.alerts.ko.title') %> </span>
          <p>
          <%= t('verification.alerts.ko.msg_html', email: @user.email) %>
          </p>
          <br>
          <br>
          <br>
          <br>
          <%= link_to t('verification.next_user'), verification_step1_path, class: "button-primary" %>
        </div>
      <% end %>
    </div>
  </div>
</div>

