<% unless cookies[:cookiepolicy].present? %>
  <div class="cookie">
    <span>
      <%= t '.cookie_html' %>
    </span>
    <a class="close" id="close_cookie" href="#">x <br><%= t '.cookie_close' %></a>
  </div>
<% end %>
