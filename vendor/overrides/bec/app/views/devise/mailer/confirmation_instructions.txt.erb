¡<%= t('bec.devise.mailer.welcome') %> <%= @email %>!

<%= t('bec.devise.mailer.confirm.p1') %>

<%= link_to t('bec.devise.mailer.confirm.link'), confirmation_url(@resource, confirmation_token: @token, locale: I18n.locale) %>

<%= confirmation_url(@resource, confirmation_token: @token, locale: I18n.locale) %>
