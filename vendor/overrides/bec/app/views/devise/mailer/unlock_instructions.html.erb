<p><%= t('bec.devise.mailer.hello') %> <%= @resource.email %>!</p>

<p><%= t('bec.devise.mailer.lock.p1') %></p>

<p><%= t('bec.devise.mailer.lock.p2') %></p>

<p><%= link_to t('bec.devise.mailer.lock.link'), unlock_url(@resource, unlock_token: @token, locale: I18n.locale) %></p>
