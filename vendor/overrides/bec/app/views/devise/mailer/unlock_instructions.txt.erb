<%= t('bec.devise.mailer.hello') %> <%= @resource.email %>

<%= t('bec.devise.mailer.lock.p1') %>

<%= t('bec.devise.mailer.lock.p2') %>

<%= link_to t('bec.devise.mailer.lock.link'), unlock_url(@resource, unlock_token: @token, locale: I18n.locale) %>

<%= unlock_url(@resource, unlock_token: @token, locale: I18n.locale) %>
