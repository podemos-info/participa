<p class="sub-h3">
<div class="box-poll">
  <div>
    <%= link_to create_vote_path(election_id: election.id) do %>
      <p class="poll-q"><%= election.full_title_for current_user %></p>
      <p class="date"><%= l election.starts_at, format: :short %> - <%= l election.ends_at, format: :short %></p>
    <% end %>
  </div>
  <% if election.has_valid_location_for? current_user %>
    <% if election.info_url? %>
      <%= link_to election.info_url, class: 'button button-info', target: '_blank' do %>
        <span>+ info</span>
      <% end %>
    <% end %>
    <%= link_to create_vote_path(election_id: election.id), class: 'button button-vote' do %>
      <span><%= t('voting.button') %></span>
    <% end %>
  <% end %>
</div>
