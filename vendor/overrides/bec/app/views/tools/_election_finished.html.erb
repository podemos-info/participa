<% content_for :title, t('voting.title') %>
<%= content_tag(:h2, t('voting.title')) %>
<div class="box">
  <div class="box-vote">
    <h3><strong><%= t('voting.finished_1') %></strong></h3>
    <p class="sub-h3"><%= raw election.close_message %></p>
  </div>
</div>

