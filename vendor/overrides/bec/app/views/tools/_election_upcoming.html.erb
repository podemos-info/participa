<% content_for :title, t('voting.title') %>
<%= content_tag(:h2, t('voting.title')) %>
<div class="box">
  <div class="box-vote">
    <h3><strong><%= t('voting.upcoming_1') %></strong></h3>
    <p class="sub-h3"><%= t('voting.upcoming_2') %> <%= l election.starts_at, format: "%-d de %B de %Y a las %H:%M" %>.</p>
  </div>
</div>

